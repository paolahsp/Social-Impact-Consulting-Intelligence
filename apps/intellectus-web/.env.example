# Leave unset to use the explicit, schema-validated local demo adapter.
# Use only the environment-specific 71_INTELLECTUS_WEB_ADAPTER webhook URL.
# Never place tokens, credentials, or other secrets in VITE_* variables.
VITE_DIAGNOSTIC_WEBHOOK_URL=
